import { Ref } from "vue";
export default function useReloadAnimation(duration: Ref<number>): {
    reloadAnimation: () => void;
    isReady: Ref<boolean>;
    isEnd: Ref<boolean>;
    onAnimationEnd: (fn: Function) => void;
};
